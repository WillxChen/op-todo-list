const performHabit = () => {
  if (habit.type === "positive") {
    /* check if habit is daily streak
        -check date
        -check last accomplished
      if daily streak then
        tally
        add one to streak

      if not streak, then 
        tally
        create new streak
    */
  }
};

const completeTask = () => {
  /*
    option 1:
      send to complete task tab
      automatically archive tasks after 30 days

    option 2:
      keep in same location and cross out the task text
      if checkbox is checked again then send to completed tasks
      this setting could be changed
  */
};
